const router = require('express').Router();
// TODO add a get route for all thoughts /api/thoughts

// TODO add a get route for single thoughts by _id /api/thoughts/:id

// TODO add a POST route to create a new thought (don't forget to push the created thought's 'id' to the associated user's 'thoughts' array field) /api/thoughts

// TODO add a PUT route to update a thought by its 'id'

// TODO add a DELETE route to remove a thought by it's id

// TODO add a POST route to create a reaction stored in a single thought's 'reactions' array field /api/thoughts/:thoughtId/reactions

// TODO add a DELETE route to pull and remove a reaction by the reaction's 'reactionId' value
module.exports = router;